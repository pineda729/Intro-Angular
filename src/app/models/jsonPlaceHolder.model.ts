import { JsonPlaceHolderService } from "../services/json-place-holder.service"

interface JasonPlaceHolderModel {
        userId: number,
        id: number,
        title: string,
        body: string
        
}
export default JasonPlaceHolderModel;